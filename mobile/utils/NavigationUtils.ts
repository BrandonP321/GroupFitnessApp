export class NavigationUtils {
    // TODO: re-evaluate later whether or not a method like this is necessary
    // public static navigate = (route: keyof typeof routes, navigation: NativeStackNavigationProp<typeof RootStackParamList, keyof typeof routes>) => {
    //     navigation.push(route)
    // }
}