import { StyleSheet } from "react-native";
import { baseUi, colors } from "~BaseUI";

export default StyleSheet.create({
    workoutCard: {
        backgroundColor: "#C5C7E8",
        height: 200,
        width: "90%",
        marginHorizontal: "auto",
        marginVertical: 10,
        padding: 20,
        borderRadius: 10
    },
    workoutTitle: {
        fontSize: 16
    }
});