import React from "react";
import { View } from "react-native";
import styles from "./BottomTabWrapper.style";

export type BottomTabWrapperScreenParamList = {
}

interface BottomTabWrapperProps {
}
const BottomTabWrapper = (props: BottomTabWrapperProps) => {
    return (
        <View/>
    )
}

export default BottomTabWrapper